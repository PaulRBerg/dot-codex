{
  "name": "@prb/dot-codex",
  "description": "PRB's .codex directory.",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "prepare": "test \"$CI\" = 'true' || husky",
    "pre-commit": "just pre-commit"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.9.6"
  }
}
